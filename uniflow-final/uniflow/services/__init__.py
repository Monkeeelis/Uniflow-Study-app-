"""Business logic, one module per feature area.

Every function takes the whole data document, mutates it in place, and returns
an optional toast (``{"message": ..., "kind": ...}``) for the browser to show.
"""
